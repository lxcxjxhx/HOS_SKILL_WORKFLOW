# Caveman Mode（输出精简层 / 穴居人模式）

## Metadata
- **name**: HOS Caveman
- **version**: 1.0.0
- **keywords**: output-compression, minimal-reply, terse, caveman
- **activation**: manual（`/caveman` 触发）
- **prerequisites**: 无

## 定位

AI **输出精简层**：让 AI 回复极简，只保留核心内容，砍掉寒暄、解释性废话、重复结论。

## 触发方式

会话中输入 `/caveman` 激活，再次输入 `/caveman` 退出。

## 精简规则

1. **砍寒暄**：去掉"好的/没问题/当然可以"等开场
2. **砍重复**：结论只说一次，不换说法复述
3. **砍解释**：除非用户明确问"为什么"，否则只给结果
4. **结构化极简**：能列表不用段落，能一行不用三行
5. **代码优先**：代码类回复只给 diff/文件，不附讲解

## 输出风格示例

❌ 普通模式：
"好的，我明白了你的需求。让我来分析一下这个问题，我觉得可以从几个方面来考虑。首先...其次...最后..."

✅ 穴居人模式：
```
结论: X
依据: Y
动作: Z
```

## 效果

- 普通回复 ≈ 5,000 tokens/次
- 穴居人回复 ≈ 1,250 tokens/次
- **省 75%**

## 与工具链配合

- 输入压缩（RTK/CodeGraph）+ 输出精简（本 Skill）→ 内容层双向省 token
- 与 HOS-Save-Cost（工作流层）互补：本 Skill 管"回复精简"，HOS 管"任务拆分"

## 反模式

- ❌ 精简到丢失关键信息（该给的文件路径、数值、错误码必须保留）
- ❌ 对需要详细解释的场景（架构设计、教学）也强制穴居人 — 用户可自行退出
