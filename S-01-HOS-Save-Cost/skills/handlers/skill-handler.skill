# Skill Handler（Skill 加载/卸载管理器）

## Metadata
- **name**: HOS Skill Handler
- **version**: 2.0.0
- **keywords**: skill-loading, skill-unloading, skill-management, context-saving
- **activation**: auto（唯一常驻 skill，约 50 tokens）
- **prerequisites**: 无

## 定位

用一个入口 Skill 管理所有其他 Skill 的加载和卸载，避免多个 Skill 的 description 常驻上下文吃掉 Token。

**核心机制**：
- 启动时只加载 skill-handler 这一个 Skill（~50 tokens）
- 需要用某个功能时，通过 `/skill load <name>` 动态加载
- 用完后通过 `/skill unload <name>` 卸载，释放上下文
- 使用 `/skill list` 查看已安装的 Skill（不加载 description）

## 命令

| 命令 | 作用 |
|------|------|
| `/skill list` | 列出所有已安装 Skill（仅名称，不加载 description） |
| `/skill load <name>` | 加载指定 Skill |
| `/skill unload <name>` | 卸载指定 Skill |
| `/skill status` | 查看当前已加载的 Skill |

## 设计原则

1. **启动时只加载 skill-handler 自身**
2. **其他 Skill 按需加载，用完即卸载**
3. **避免多个 Skill 的 description 同时占用上下文**
4. **MCP 层面不介入**：MCP 工具已被 mcp-compressor 压缩成 2 个入口，无需本 Skill 管理

## 效果

- 5 个 Skill description 常驻 ≈ 5,000 tokens
- 改为 skill-handler 常驻 + 按需加载 ≈ 50 tokens
- **省 90%**

## 注册表（本项目内置 Skill）

| 名称 | 文件 | 用途 |
|------|------|------|
| `hos` | HOS-Save-Cost（SKILL.md） | 工作流层省钱模式 |
| `task-split` | skills/workflow/task-split.skill | 任务拆分 |
| `handoff` | skills/workflow/handoff.skill | 交接文档 |
| `token-monitor` | skills/workflow/token-monitor.skill | Token 监控 |
| `caveman` | skills/style/caveman.skill | 输出精简 |
| `tfe` / `anpe` / `mra` / `cce` | skills/*.skill | 方法层四技能 |

## 反模式

- ❌ 把所有 Skill 一次性加载（回归 5,000 tokens 常驻）
- ❌ 卸载后仍残留 description 在上下文中
- ❌ 加载未安装的 Skill 名称（先 `/skill list` 确认）
