# Token Router（任务类型 → 最省工具/模型 的路由控制器）

## Metadata
- **name**: Token Router
- **version**: 1.0.0
- **keywords**: routing, model-selection, tool-selection, cheap-first, layer-router
- **activation**: manual（`/skill load router`，建议在会话规划阶段启用）
- **prerequisites**: token-monitor 推荐（本 skill 决定"怎么走"，monitor 决定"剩多少"）

## 定位

TOKEN-SAVIOUR 式路由：**让每类任务走最省 Token 的那条路**，而不是默认所有任务都上强模型/全量工具。

HOS 不自研一个笨重调度器，而是给一套**静态决策表** + 触发规则：
- **工具层路由**：代码探索该用 grep 还是 codegraph？命令输出要不要打进上下文？
- **模型层路由**：这件活是否值得贵模型？还是便宜模型就够？

> 原则：**贵模型只处理贵模型不可替代的问题。**
> 简单任务跑贵模型 = 每 Token 都在付冤枉钱。

## 一、工具层路由表（读用最小代价）

| 任务类型 | 用 | 别用 |
|---------|----|------|
| 查符号/调用关系（大库） | `codegraph`（预索引） | 逐个 `read` 整文件 |
| 精确小范围改 | `read` 目标文件（TFE 限 2-5 文件） | `grep` 全库 + 读整树 |
| 端到端验证 | 局部 `git diff` / 单文件 | `read` 全项目 |
| 命令输出 | RTK（压缩） | 原始 stdout 全量进上下文 |
| 跨模块依赖 | 先 codegraph 图，再定点 read | 随机点文件 |

**裁决规则**：
- `codegraph` 有索引 → 优先它；索引缺失 → 先 `codegraph index`（一次性），不要退回笨读。
- 阶段化：**先结构后实现**。先读依赖图/接口（几百行），确定热点了再读那 1 个文件，别一开始就读实现。

## 二、模型层路由表（贵模型稀缺资源）

| 任务类型 | 推荐模型档 | 说明 |
|---------|-----------|------|
| 文件查找 / grep | 本地/规则 | 零模型 |
| 简单字段修改 / 拼装 | 便宜快 | flash 级够 |
| 单元测试编写 | 便宜快 | 模板化高 |
| 普通编码（小-中） | 中型 | 平衡 |
| 架构评审 / 安全分析 / 复杂调试 | 强推理 | 贵模型不可替代 |
| 长链条多步代理 | 中-强 | 看单步复杂度 |

**切档信号**（配合 token-monitor）：
- 会话累计 < 40% 预算 → 可维持当前档
- 40-70% → 检查是否有子任务能下放到便宜档
- 70-85% → 强制把剩余简单子任务切便宜档
- >85% → 剩复杂活强档 + 平凡活全切便宜/本地

## 三、路由输出格式（会话规划时输出一次）

```text
## Route Plan

- 代码探索: [codegraph / 定点 read]
- 命令输出: [RTK 开 / 不开]
- 模型档: [便宜 / 中型 / 强推理]
- 理由: [一句话：为什么这档够/不足]
- 备降路径: [若当前档不够，切 [更贵档]]
```

## 四、与技能体系配合

- `token-monitor` → 提供剩余预算，驱动切档
- `task-split` / `handoff` → 输出路由所需的子任务边界
- `ponytail` → 降低写码 token，变相降低所需模型档
- `caveman` → 输出精简，降低每条回复成本

## 五、反模式

- ❌ 所有任务默认强模型（贵，且大多数没必要）
- ❌ 路由表本身过度设计（本 skill 是轻决策表，不是真实调度服务）
- ❌ 频繁切档打断流程（只在规划点或 monitor 阈值点切一次）

## Related Skills
- token-monitor（预算监控 / 切档信号）
- task-split（子任务边界）
- ponytail（减少写码成本）
- caveman（减少输出成本）
