# Ponytail Restraint（代码生成克制 / YAGNI 决策阶梯）

## Metadata
- **name**: Ponytail Restraint
- **version**: 1.0.0
- **keywords**: yagni, minimum-viable-code, restraint, no-over-engineering, decision-ladder
- **activation**: manual（`/skill load ponytail`，建议在写代码阶段随驻 TFE 一起）
- **prerequisites**: TFE skill 推荐（本 skill 管"少写"，TFE 管"少读/结构"）

## 定位

TFE/MRA 已经解决了**读得太少**的问题（少读代码、压缩输入）。
本 skill 解决**写得太多的另一半**：AI 动不动重写/造轮子/加抽象，把简单事做复杂。

> **核心信念：用户只让改一个东西，AI 却开始造框架。**
> 最好的代码是你根本没写的代码。

与 caveman 互补但不重叠：
- `caveman` → 少**说**（输出精简）
- `RTK` → 少**读**（命令输出压缩）
- `ponytail` → 少**写**（代码生成克制）

## 决策阶梯（自上而下，卡住即停）

接到任何编码任务，**从第 1 级开始**，问一句，能停就停：

```
1. 这个任务需要存在吗？            (YAGNI：不做)
2. 已有的代码能直接复用吗？        (改 > 新增)
3. 标准库能搞定吗？                (不进依赖)
4. 平台原生功能能搞定吗？          (不引框架)
5. 现有依赖里有没有现成的？        (引旧 > 引新)
6. 第三方库能解决 90%+？           (用库改配置 > 手写)
7. 以上都没有，才写新代码。        (而且只要能跑的最小版本)
```

**纪律**：每往下一级，都多花一次 Token 成本。停在越靠上的级，省得越多。

## 五条硬规则

1. **一行为一句**：能一行绝不用函数，能函数绝不用类，能类绝不用框架。
2. **不造抽象**：一个调用点不要 factory/interface/DI；两个以上调用点再考虑。
3. **YAGNI 默认"不做"**：不确定要不要的功能，默认砍掉，除非用户点名要。
4. **最小 diff**：只动被要求的那几行，灰色地带交给用户，不擅自重构。
5. **删除优先**：改 bug 前先问"这行逻辑能不能删"，删 > 注释掉 > 重写。

## 触发时输出格式（写码前）

```text
## Restraint Check

- 任务必要性: [必需 / 可砍 - 理由]
- 已有复用: [无 / 文件:行号]
- 标准库: [无需 / 覆盖 - 名字]
- 新依赖: [无 / 需引入 - 名字 + 理由]
- 最终动作: [删除 / 复用 / 一行改造 / 最小新增]
- 预计代码: [X 行（<= 需要的最小值）]
```

## 与现有技能的分工

| 技能 | 管什么 | 触发 |
|------|--------|------|
| `TFE` | 少读代码、结构精简、配置化 | 每个任务随驻 |
| `MRA` | 遗留系统少改、保护核心路径 | 重构/接盘 |
| `CCE` | 大库上下文压缩 | 100K+ LOC |
| **`ponytail`** | **代码生成克制、不造轮子** | 写任何代码时 |

## 反模式

- ❌ 为了优雅加一层抽象（一个调用点禁止）
- ❌ 复制第三方库代码到自己项目（"同步一下"也是新债）
- ❌ 预写未来才用到的参数/标志位/分支
- ❌ 把重构当成任务的一部分（用户只让改 bug）

## Related Skills
- TFE（Token First Engineering）：结构/预算
- MRA（Minimal Refactor Architecture）：遗留系统省改
- CCE（Context Compression Engineering）：大库上下文
- caveman（输出精简）：少说
