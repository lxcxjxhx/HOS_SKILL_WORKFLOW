# Handoff Document Protocol（交接文档协议）

## Metadata
- **name**: HOS Handoff
- **version**: 2.0.0
- **keywords**: handoff, task-transition, state-transfer, context-handover
- **activation**: manual（子任务完成时自动输出）
- **prerequisites**: HOS-Save-Cost

## 目标

子任务完成后，用一份**紧凑的交接文档**把状态传递给下一个子任务/下一个会话。下一个子任务**只加载交接文档**，不重复加载历史上下文。

## 交接文档模板

```markdown
## HANDOFF <子任务编号>

### 状态
- 完成情况: [完成 / 部分完成 / 阻塞]
- 产出物: [文件/结果清单]

### 关键决策
- 决策 1: ...（原因）
- 决策 2: ...

### 下一步
- [下一步动作 1]
- [下一步动作 2]

### 未决问题 / 风险
- [问题 1]
- [问题 2]

### 上下文快照（供下一子任务加载）
- 相关文件: [路径列表]
- 关键结论: [3 行以内]
- 遗留 Token: [本子任务实际消耗]
```

## 规则

1. **必须极简**：整个交接文档 ≤ 500 tokens（目标 200 tokens）
2. **必须可独立理解**：下一子任务不读历史也能继续
3. **只传状态，不传过程**：不要复制大段代码/日志
4. **关键路径可引用**：用文件路径 + 行号代替内容复制
5. **标记阻塞点**：阻塞时明确写出需要的输入

## 使用流程

```
子任务 N 完成
  ↓ 输出 HANDOFF N（≤500 tokens）
子任务 N+1 启动
  ↓ 只加载 HANDOFF N（+ 需要的最小文件）
  ↓ 执行
  ↓ 输出 HANDOFF N+1
```

## 成本对比

- 无交接文档：子任务 N+1 重新加载全部历史 → 上下文爆炸
- 有交接文档：子任务 N+1 只加载 200~500 tokens 状态 → 省 80%+

## 反模式

- ❌ 交接文档写成长篇报告（>1K tokens，违背目的）
- ❌ 复制完整 diff / 日志进交接文档
- ❌ 不标记阻塞点，让下一子任务盲目重试
