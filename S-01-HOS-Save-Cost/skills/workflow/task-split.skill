# Task Split Protocol（任务拆分协议）

## Metadata
- **name**: HOS Task Split
- **version**: 2.0.0
- **keywords**: task-splitting, context-isolation, sub-task, workflow
- **activation**: manual（由 HOS-Save-Cost 激活时调用）
- **prerequisites**: HOS-Save-Cost

## 目标

把大任务拆成多个**独立上下文**的子任务，避免单个会话上下文爆炸。每个子任务在自己的上下文里完成，通过交接文档传递状态。

## 触发条件（需求识别）

出现以下任一场景即应拆分：

- 任务估计 > 50K tokens 或需要读取 > 10 个文件
- 任务包含多个不相关的阶段（调研 → 设计 → 实现 → 测试 → 部署）
- 任务需要长时间多轮对话，历史会无限累积
- 任务包含探索性部分（不确定答案，需要试错）

## 拆分规则

1. **按阶段拆**：调研/设计/实现/测试/文档各成子任务
2. **按模块拆**：涉及多个独立模块时，每个模块一个子任务
3. **按依赖拆**：无依赖的子任务可并行；有依赖的按序执行，靠交接文档衔接
4. **每个子任务独立上下文**：完成后不保留历史，只保留交接文档
5. **单子任务 token 上限**：默认 ≤ 15K tokens，超过则继续拆分

## 拆分输出格式

在拆分完成时输出：

```
## Task Split Plan

总任务: [一句话描述]
预计总 Token: [X tokens]

子任务列表:
1. [子任务 1]
   目标: ...
   输入: ...
   输出: ...
   预计 Token: [X]
   依赖: [无 / 子任务 N]

2. [子任务 2]
   ...

交接链: 子任务1 → 子任务2 → ...（每步产出交接文档）
```

## 与工具链配合

- 拆分前先做 **需求识别**（识别成本敏感场景）
- 拆分后每个子任务执行时应用 **TFE**（方法层）做 Token 估算
- 子任务完成产出 **交接文档**（见 handoff.skill）
- 全程由 **token-monitor.skill** 监控消耗

## 反模式

- ❌ 拆成 50 个碎片任务（交接文档成本超过收益）— 3~7 个为宜
- ❌ 子任务间共享上下文（等于没拆）
- ❌ 拆分后仍让一个会话做完全部子任务（失去意义）
